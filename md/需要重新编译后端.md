# 🔴 重要：需要重新编译后端

## 问题现状

经过深入排查，所有代码修改都已完成，但 **JAR 文件是旧版本**，没有包含最新的 CORS 配置。

- JAR 文件创建时间：19:17
- 当前时间：19:55+
- **结论**：代码修改后没有重新编译

## 解决方案

### 方案 1：使用 IDEA 重新编译并运行（推荐）

1. **打开 IDEA**
2. **打开项目**：`/Users/macbook/前端项目/灵月仙途/lingyuexiantu-server`
3. **等待 Maven 同步完成**
4. **找到主类**：`com.lingyue.LingyuexiantuServerApplication`
5. **右键 → Run**

IDEA 会自动重新编译并运行，包含所有最新的 CORS 配置。

### 方案 2：安装 Maven 后重新打包

```bash
# 安装 Maven
brew install maven

# 重新打包
cd /Users/macbook/前端项目/灵月仙途/lingyuexiantu-server
mvn clean package -DskipTests

# 运行
java -jar target/lingyuexiantu-server-0.0.1-SNAPSHOT.jar
```

### 方案 3：临时使用已编译的 class 文件运行

如果你不想重新编译，可以使用 IDEA 直接运行编译后的 class 文件：

1. 在 IDEA 中打开项目
2. 找到 `com.lingyue.LingyuexiantuServerApplication`
3. 直接 Run（不需要打包）

## 已完成的代码修复

以下文件已修改，重新编译后即可生效：

### 1. GlobalCorsConfig.java
```java
.allowedOrigins(
    "http://localhost:5502",
    "http://127.0.0.1:5502",
    "http://192.168.110.98:5502"  // 你的 IP
)
```

### 2. SecurityConfig.java
```java
.cors(cors -> cors.disable())  // 禁用 Security 的 CORS，使用 GlobalCorsConfig
```

### 3. index.html
```html
<script src="js/config.js"></script>  <!-- 已添加 -->
```

### 4. js/config.js
```javascript
// 自动检测局域网 IP 并切换 API 地址
```

## 验证步骤

重新编译并启动后，在手机上访问：

1. **访问诊断页面**：`http://192.168.110.98:5502/diagnose-network.html`
2. **点击"运行完整诊断"**
3. **所有测试应该显示 ✅**

然后访问主页：`http://192.168.110.98:5502`

## 如果仍然失败

1. **清除手机浏览器缓存**
2. **检查后端日志**：
   ```bash
   tail -f /Users/macbook/前端项目/灵月仙途/lingyuexiantu-server/logs/lingyuexiantu.log
   ```
3. **查看是否有 CORS 相关的错误**

## 联系支持

如果按照上述步骤操作后仍有问题，请提供：
- 后端日志
- 手机浏览器 Console 截图
- 诊断页面的测试结果
